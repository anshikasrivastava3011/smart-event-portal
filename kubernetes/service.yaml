apiVersion: v1
kind: Service

metadata:
  name: eventportal-service

spec:
  type: NodePort

  selector:
    app: eventportal

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080